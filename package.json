{
  "name": "blitzhub-mobile-updated",
  "version": "1.0.0",
  "description": "A serverless contact form and website project for BlitzHub",
  "main": "api/sendForm.js", // Entry point for backend logic
  "dependencies": {
    "node-fetch": "^3.3.1" // Used for making HTTP requests, if needed
  },
  "devDependencies": {
    "nodemon": "^2.0.21" // For local development to auto-restart the server
  },
  "scripts": {
    "start": "vercel dev", // Start the Vercel development environment
    "dev": "nodemon api/sendForm.js", // Optional: Run the project locally without Vercel
    "build": "echo 'No build step required for this project'"
  },
  "engines": {
    "node": ">=14.0.0" // Ensure compatibility with the Node.js runtime
  },
  "author": "fidelnyame-80",
  "license": "MIT"
}
